{
  "name": "@codex-swarm/landing",
  "version": "0.1.0",
  "private": true,
  "description": "First-pass Eleventy landing page for Codex Swarm",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "rm -rf _site && eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
